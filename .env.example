# Claude Code UI Environment Configuration
# Copy this file to .env and update the values as needed

# Server Configuration
PORT=3001
NODE_ENV=production

# Authentication
# Generate a secure JWT secret for authentication
# You can use: openssl rand -base64 32
JWT_SECRET=your-secure-jwt-secret-here

# Optional: OpenAI API Key for Whisper transcription
# Leave empty if you don't need audio transcription features
OPENAI_API_KEY=

# Database Configuration
# SQLite database will be stored in ./data/db/auth.db
# No additional configuration needed for SQLite

# Claude CLI Configuration
# The container will use your host's ~/.claude directory
# Make sure you have Claude CLI installed and configured on your Mac

# Cursor CLI Configuration
# The container will use your host's ~/.cursor directory
# Make sure you have Cursor CLI installed and configured on your Mac

# Logging
LOG_LEVEL=info

# Development Settings (for local development only)
# VITE_PORT=5173
